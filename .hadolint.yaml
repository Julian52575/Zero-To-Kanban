# hadolint config, read by the `dockerfile-lint` job in .github/workflows/ci.yml.

ignored:
  # DL3008 (pin apt package versions): Debian only serves the latest security
  # build of a package, so a pinned version breaks the build on every update.
  # The base image tag is what fixes the package set.
  - DL3008
